apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sqlbot.fullname" . }}-config
  labels:
    {{- include "sqlbot.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
